name: CI

on: [push]

jobs:
  build:

    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [windows-2019, or windows-2016]
        
    steps:
#    - uses: actions/checkout@v1
    - name: Run a multi-line script
      run: |
        tree "C:\Program Files (x86)\Microsoft Visual Studio"
